version: '3'
services:

  proxy:
    build:
      context: ./envoy
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
  server:
    build:
      context: ./audioNode
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - proxy

  web-client:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "4003:4003"
    depends_on:
      - server
      - proxy
    tty: true
