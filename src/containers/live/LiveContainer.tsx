import React from 'react';
import Live from '../../components/live/Live';

export const LiveContainer = (props : any) => {
  return (
         <div className='container'>
           <Live/>
         </div>

  );
}
