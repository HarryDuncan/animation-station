// Returns boolean if bg is dark or not
export const getBGDark = (bgColor : string) => {
  return true
}

// Safely retrieve background color
export const retrieveBG = (sceneParams : any) => {
 return '0x050505'
}

// Safely retrieve the scene time length
export const retrieveSceneTime = (sceneParams : any) => {
  return 10000
}

//
